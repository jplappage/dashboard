# Letterboxd slugs to NEVER auto-add to films-data.js.
# Use for films removed on purpose — e.g. a title match pulled in the
# wrong-year version of a remake, or the old original alongside it.
# One Letterboxd slug per line. Blank lines and #comments are ignored.
# Respected by update_new_films.py AND the manual refresh (see REFRESH_INSTRUCTIONS.md).
